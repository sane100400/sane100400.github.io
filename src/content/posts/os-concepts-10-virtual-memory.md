---
title: "공룡책 운영체제 정리 10: 가상 메모리"
published: 2026-05-21
description: "Operating System Concepts 10판 Ch.10 가상 메모리 정리"
tags: [Operating System, OS, 공룡책, CS]
category: "Computer Science"
draft: false
---

`Operating System Concepts` 10판 Chapter 10, **Virtual Memory** 정리다.

공룡책 10판 기준 PDF p.501 근처에서 시작한다. 가상 메모리가 프로그램 일부만 메모리에 올리는 방식부터 요구 페이징과 page fault 처리 흐름까지 다룬다.

## 먼저 볼 부분

- 가상 메모리가 프로그램 일부만 메모리에 올리는 방식
- 요구 페이징과 page fault 처리 흐름
- FIFO, Optimal, LRU 계열 페이지 교체 정책
- 프레임 할당, working set, thrashing

## 가상 메모리의 출발점

가상 메모리는 프로그램 전체가 물리 메모리에 있어야 실행된다는 생각을 버린다. 실제로 실행에 필요한 부분은 특정 시점의 일부 페이지뿐이다. 나머지는 디스크에 있다가 접근될 때 메모리로 올라올 수 있다. 이 방식은 큰 프로그램 실행, 여러 프로세스 동시 실행, 프로세스 간 공유를 쉽게 만든다.

다만 가상 메모리는 무한 메모리가 아니다. 물리 메모리와 저장장치 사이를 오가며 착시를 만드는 구조다. page fault가 잦아지면 성능은 급격히 떨어진다.

## 요구 페이징의 흐름

페이지 테이블에는 해당 페이지가 메모리에 있는지 나타내는 valid-invalid bit가 있다. 프로세스가 invalid 페이지에 접근하면 page fault trap이 발생한다. 커널은 접근이 합법적인지 확인하고, 빈 frame을 찾은 뒤, 디스크에서 페이지를 읽어 오고, 페이지 테이블을 갱신한 다음 중단된 명령을 다시 실행한다.

이 흐름은 I/O를 포함하므로 일반 메모리 접근보다 훨씬 비싸다. 그래서 page fault rate를 낮추는 것이 가상 메모리 성능을 크게 좌우한다.

![가상 메모리 - 요구 페이징 처리](/os-concepts/diagrams/os-10-virtual-memory.png)

> 그림 읽기: valid bit가 꺼진 페이지에 접근하면 page fault가 난다. 커널은 합법성 검사, 프레임 확보, 디스크 읽기, 페이지 테이블 갱신을 거쳐 중단된 명령을 다시 실행한다.

![가상 메모리 - Page fault 비용](/os-concepts/diagrams/os-10-virtual-memory-detail.png)

> 그림 읽기: page fault 비용은 trap만이 아니라 디스크 I/O를 포함한다. 그래서 페이지 교체 정책과 working set 관리가 성능에 직접 영향을 준다.

## 페이지 교체 정책

빈 frame이 없으면 어떤 페이지를 내보낼지 정해야 한다. FIFO는 오래 들어와 있던 페이지를 내보내지만, 자주 쓰이는 페이지도 오래됐다는 이유로 쫓아낼 수 있다. Optimal은 앞으로 가장 늦게 사용될 페이지를 내보내므로 기준점으로는 좋지만 미래를 알아야 하므로 실제 구현은 어렵다.

LRU는 최근에 사용되지 않은 페이지가 앞으로도 덜 쓰일 것이라는 지역성에 기대는 정책이다. 정확한 LRU 구현은 비용이 크기 때문에 reference bit, clock algorithm 같은 근사 기법이 자주 쓰인다.

## Thrashing과 working set

프로세스에 할당된 frame이 작업 집합보다 적으면 필요한 페이지를 계속 쫓아내고 다시 가져오게 된다. 이때 CPU는 계산보다 페이지 교체와 디스크 I/O에 시간을 쓰며, 시스템 전체 처리량이 떨어진다. 이것이 thrashing이다.

working set model은 일정 시간 창 안에서 실제로 사용한 페이지 집합을 추적해 프로세스가 필요한 메모리 규모를 추정한다. 프로세스 수를 무작정 늘리는 것이 항상 좋은 일은 아니라는 점도 봐야 한다.

## 예시: 처음 접근한 페이지가 메모리에 없을 때

1. CPU가 invalid 페이지를 참조하면 하드웨어가 trap을 걸고 커널로 들어간다.
2. 커널은 접근이 합법인지 확인한 뒤 빈 프레임을 찾거나 희생 페이지를 고른다.
3. 디스크에서 페이지를 읽고 페이지 테이블을 고친 다음, 중단됐던 명령을 다시 실행한다. 그래서 page fault는 일반 메모리 접근보다 훨씬 비싸다.

## 용어 정리

### 가상 메모리

- 의미: 물리 메모리보다 큰 주소 공간을 쓰는 것처럼 보이게 하는 기법.
- 기능: 프로그램 일부만 메모리에 올려 메모리 사용 효율을 높인다.
- 주의: 무한 메모리를 제공하는 기술은 아니다.
- 키워드: `virtual memory`

### 요구 페이징

- 의미: 접근한 페이지가 필요할 때만 메모리에 올리는 방식.
- 기능: 초기 적재 비용과 메모리 사용량을 줄인다.
- 주의: page fault가 자주 나면 성능이 크게 떨어진다.
- 키워드: `demand paging`

### 페이지 폴트

- 의미: 접근한 페이지가 메모리에 없을 때 발생하는 트랩.
- 기능: 커널이 페이지를 디스크에서 읽어 오도록 만든다.
- 주의: 일반 캐시 미스보다 훨씬 비싸다.
- 키워드: `page fault`

### 페이지 교체

- 의미: 빈 프레임이 없을 때 내보낼 페이지를 고르는 작업.
- 기능: FIFO, LRU, Optimal 같은 정책으로 fault 수를 줄인다.
- 주의: Optimal은 실제 구현보다 비교 기준이다.
- 키워드: `FIFO`, `LRU`

### Thrashing

- 의미: 계산보다 페이지 교체에 시간을 더 쓰는 상태.
- 기능: working set과 page-fault frequency로 감지한다.
- 주의: 프로세스를 더 넣으면 오히려 악화될 수 있다.
- 키워드: `thrashing`, `working set`

## 조심할 부분

- page fault는 단순 캐시 미스보다 훨씬 비싼 예외 처리와 I/O를 포함한다.
- Optimal 교체는 구현 알고리즘이라기보다 비교 기준에 가깝다.
- 멀티프로그래밍 정도를 높이면 어느 순간 thrashing으로 전체 성능이 무너질 수 있다.

## 이어지는 내용

메모리 계층을 이해했으니, 11장부터는 데이터를 오래 보관하는 저장장치 구조로 내려간다.
