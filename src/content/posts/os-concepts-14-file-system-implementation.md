---
title: "공룡책 운영체제 정리 14: 파일 시스템 구현"
published: 2026-05-17
description: "Operating System Concepts 10판 Ch.14 파일 시스템 구현 정리"
tags: [Operating System, OS, 공룡책, CS]
category: "Computer Science"
draft: false
---

`Operating System Concepts` 10판 Chapter 14, **File-System Implementation** 정리다.

공룡책 10판 기준 PDF p.694 근처에서 시작한다. 파일 시스템 계층 구조부터 디렉터리 구현과 FCB/inode까지 차례로 정리한다.

## 먼저 볼 부분

- 파일 시스템 계층 구조
- 디렉터리 구현과 FCB/inode
- 연속, 연결, 색인 할당 방식
- 자유 공간 관리, 성능, 복구

## 파일 시스템은 여러 층으로 구현된다

사용자는 파일 이름과 read/write만 보지만, 내부에는 논리 파일 시스템, 파일 조직 모듈, 기본 파일 시스템, I/O 제어 계층이 있다. 위쪽은 이름과 메타데이터를 다루고, 아래쪽은 블록 번호와 장치 I/O를 다룬다.

이 계층화 덕분에 파일 API와 실제 장치 제어를 분리할 수 있다. 파일 시스템 구현을 읽을 때는 이름 처리, 메타데이터 처리, 블록 배치, 장치 접근을 나눠서 보는 것이 좋다.

## FCB와 inode

파일 제어 블록은 파일의 메타데이터와 데이터 블록 위치 정보를 담는다. UNIX 계열에서는 inode가 이 역할을 한다. 디렉터리 엔트리는 보통 파일 이름과 inode 번호를 연결하고, inode는 실제 블록 위치와 권한, 크기, 시간 정보를 가진다.

이 구조를 이해하면 hard link도 자연스럽게 보인다. 여러 이름이 같은 inode를 가리킬 수 있고, inode의 link count가 0이 되면 실제 데이터 회수가 가능해진다.

![파일 시스템 구현 - inode와 블록 매핑](/os-concepts/diagrams/os-14-file-system-implementation-detail.png)

## 블록 할당 방식

연속 할당은 파일 블록을 디스크의 연속된 위치에 둔다. 순차 접근과 직접 접근이 빠르지만, 파일이 커질 때 주변에 빈 공간이 없으면 문제가 생긴다. 연결 할당은 각 블록이 다음 블록을 가리키므로 파일 확장은 쉽지만 임의 접근이 느리다.

색인 할당은 인덱스 블록이 데이터 블록 주소들을 담는다. 임의 접근과 확장에 유리하지만 인덱스 블록 비용이 든다. 실제 파일 시스템은 작은 파일과 큰 파일 모두를 고려해 직접 포인터, 간접 포인터 등을 조합한다.

![파일 시스템 구현 - 파일 블록 할당 방식](/os-concepts/diagrams/os-14-file-system-implementation.png)

## 자유 공간, 성능, 복구

새 블록을 할당하려면 빈 블록을 알아야 한다. bitmap은 각 블록의 사용 여부를 비트로 표시해 단순하고 빠르게 탐색할 수 있다. free list는 빈 블록들을 연결해 관리한다. 어떤 방식이든 동시 갱신과 crash 상황을 고려해야 한다.

파일 시스템은 캐시와 버퍼를 통해 성능을 높인다. 하지만 쓰기 캐시가 있으면 갑작스러운 전원 차단에서 메타데이터와 데이터가 엇갈릴 수 있다. journaling과 log-structured 접근은 이런 복구 문제를 줄이기 위한 설계다.

## 용어 정리

### 파일 시스템 계층

- 의미: 파일 API부터 블록 I/O까지 이어지는 구현 층.
- 기능: 이름, 메타데이터, 블록 매핑, 장치 접근을 분리한다.
- 주의: 파일 시스템과 장치 드라이버를 같은 층으로 보면 안 된다.
- 키워드: `VFS`, `block`

### FCB/inode

- 의미: 파일의 메타데이터와 블록 위치를 담는 구조.
- 기능: 파일 이름과 실제 데이터 블록 사이를 연결한다.
- 주의: UNIX 계열에서는 inode 개념이 핵심이다.
- 키워드: `inode`, `FCB`

### 연속 할당

- 의미: 파일 블록을 연속된 위치에 배치하는 방식.
- 기능: 순차/직접 접근이 빠르다.
- 주의: 외부 단편화와 파일 성장 문제가 있다.
- 키워드: `contiguous`

### 연결 할당

- 의미: 각 블록이 다음 블록을 가리키는 방식.
- 기능: 파일 확장이 쉽고 외부 단편화가 적다.
- 주의: 임의 접근이 느리고 포인터 손상에 취약하다.
- 키워드: `linked`, `FAT`

### 색인 할당

- 의미: 인덱스 블록이 데이터 블록 주소들을 담는 방식.
- 기능: 임의 접근과 파일 확장에 유리하다.
- 주의: 작은 파일에서도 인덱스 블록 비용이 생길 수 있다.
- 키워드: `indexed`

### 자유 공간 관리

- 의미: 빈 블록을 추적하는 방식.
- 기능: 새 파일이나 확장에 사용할 공간을 빠르게 찾는다.
- 주의: bitmap은 단순하지만 크기와 동기화 비용이 있다.
- 키워드: `bitmap`, `free-space`

## 조심할 부분

- 디렉터리는 파일 내용을 담는 곳이 아니라 이름을 파일 식별자에 연결하는 구조다.
- 연결 할당은 외부 단편화는 줄이지만 임의 접근 비용이 크다.
- 저널링은 모든 사용자 데이터를 항상 완벽히 보존한다는 뜻이 아니다.

## 이어지는 내용

14장에서 한 파일 시스템 내부를 봤다면, 15장은 여러 파일 시스템을 마운트하고 공유하는 내부 구조로 넓힌다.
